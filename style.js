import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 32,
    },
    textInputStyle: {
        textAlign: 'center',
        height: 40,
        borderRadius: 10,
        borderBottomWidth: 2,
        borderBottomColor: '#CDBDBD',
        marginBottom: 10,
    },
});

export default styles;